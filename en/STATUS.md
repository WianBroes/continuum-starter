# STATUS — open points of the system

> Reserved for the Continuum system itself (ritual, tools, shared files); a project's points go in its `CHANGELOG.md`. Not read at startup — looked at when closing and on request.
> A line arrives through the `## STATUS +` section of a closed session. Correcting or removing a line: by hand, under lock. A resolved point is removed (the history is in `BASE.md`).

| Point | Open since | State |
|---|---|---|
