# BASE — shared memory

> Written only by the merge (`tools/consolidate.sh`, under lock — `AGENTS.md` §3): the `## For BASE` (§History) and `## Durable facts` (§Decided) sections of closed sessions. Caps applied at every merge. Rewritten by hand only under lock.

## Decided

> Cap of 5 entries, 3 lines each at most. Surplus → `_archive/`, verbatim.

*(empty — first close to come)*

## History

> Cap of 5 entries. Surplus purged to `_archive/`, oldest (by start date) first — last purge: none.

*(empty — first close to come)*
