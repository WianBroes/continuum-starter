# TRAITS — the user's behavioral traits

> Traits **cross-checked** over at least 3 distinct sessions (`protocols/learning.md` step 5) — not direct statements (those go in `PROFILE.md`). How they check, decide, design, correct. Informs how their requests are read; does not prescribe the agent's behavior (that is `DIRECTIVES.md`).
> A trait = one sentence stating it + a **Basis** (sessions/dates of the confirmations, 2-3 lines). Merged, never appended — written under lock.

## Traits

*(empty — nothing cross-checked yet.)*

## Revisions

> A trait contradicted by use is revised directly by the loop (old text archived) and announced; the user can refuse. Additions marked `[auto YYYY-MM-DD — not announced]` as in `DIRECTIVES.md`. Nothing here yet.
